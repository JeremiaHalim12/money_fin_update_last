import 'package:cloud_firestore/cloud_firestore.dart';

class HitungTotalTabungan {
  // CollectionReference total = FirebaseFirestore.instance.collection("");

  Future<void> getAllCollection(String uid) async {
    


    // DocumentSnapshot snapshot =
    //     await FirebaseFirestore.instance.collection("user").doc(uid).get();

    // if (snapshot.exists) {
    //   QuerySnapshot querySnapshot =
    //       await snapshot.reference.collection(collectionName).get();

    //   for (QueryDocumentSnapshot docSnapshot in querySnapshot.docs) {
    //     Object? data = snapshot.data();
    //     print(data);
    //   }
    // }

    // String collectionName;
    // // print(snapshot);
    // for (QueryDocumentSnapshot docSnapshot in snapshot.docs) {
    //   collectionName = docSnapshot.reference.path;
    //   print(collectionName);
    // }
  }
}
